[agent]
# cursor (command: cursor-agent) | grok | pi | codex | opencode | claude | <custom>
command = "cursor-agent"

[layout]
# refresh-review adds --watch
review = "hunk diff"
# Sidebar file opens always run: fresh <path>
editor = "fresh"
