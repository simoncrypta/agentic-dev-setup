{
  "name": "agentic-dev-setup",
  "private": true,
  "scripts": {
    "predeploy": "bash scripts/sync-public.sh",
    "deploy": "wrangler pages deploy public --project-name=agentic-dev-setup"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
